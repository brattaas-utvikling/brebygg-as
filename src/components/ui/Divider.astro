---
// src/components/ui/Divider.astro
// Kort horisontal skillelinje i Green Leaf.
// Plasseres typisk under seksjonstittelen.

interface Props {
  centered?: boolean;
  class?:    string;
}

const { centered = false, class: extra = "" } = Astro.props;
---

<div
  class={`divider ${centered ? "divider--center" : ""} ${extra}`}
  aria-hidden="true"
></div>
